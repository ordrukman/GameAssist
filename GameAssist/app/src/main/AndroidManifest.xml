<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gameassist">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/vs_ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/vs_ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GameAssist">
        <activity
            android:name=".ActivityResults"
            android:exported="true" />
        <activity
            android:name=".ActivityMatches"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>